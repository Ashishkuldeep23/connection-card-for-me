import React from 'react'

const CardTypeDiv = ({cType = ""}) => {
    return (
        <>


            <div id="heading_div">
                <h2>{`Let's Connect ${cType}`}</h2>
            </div>

        </>
    )
}

export default CardTypeDiv